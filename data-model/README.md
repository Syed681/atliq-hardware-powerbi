# Data Model

The Power BI project uses a dimensional data model connecting business fact tables with common dimensions such as Date, Customer, Product, and Market.

The complete schema is shown below.

![AtliQ Hardware Data Model](  )
